"""Dashboard assets for the CoCo Trust Layer gateway.

Templates live in `dashboard/templates/` and are served by
`routes.dashboard`. Static CSS/JS live in `dashboard/static/` and are
mounted at `/static` by `main.py` via `StaticFiles`.
"""
